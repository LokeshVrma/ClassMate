apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "classmate-backend-helm.fullname" . }}-test-connection"
  labels:
    {{- include "classmate-backend-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "classmate-backend-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
